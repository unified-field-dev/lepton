# Examples

| Example | Purpose |
|---------|---------|
| `email_send_record_smoke` | Boot mem Spectra, record one `lepton_email_send`, query points |

```bash
cargo run -p lepton-spectra-telemetry --example email_send_record_smoke
```
