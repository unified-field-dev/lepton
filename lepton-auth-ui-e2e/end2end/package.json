{
  "name": "lepton-auth-ui-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "wait-server": "wait-on -t 180000 -i 500 -l http-get://localhost:3120/health",
    "test": "npm run wait-server && npx playwright test --workers=1",
    "test:oauth-link": "npm run wait-server && npx playwright test oauth_link_settings.spec.ts --workers=1",
    "test:wipe": "npm run wait-server && npx playwright test account_wipe.spec.ts --workers=1",
    "test:step-up": "npm run wait-server && npx playwright test step_up_modal.spec.ts --workers=1",
    "test:headed": "npm run wait-server && PW_HEADED=1 npx playwright test --workers=1 --headed",
    "test:ui": "npm run wait-server && npx playwright test --ui --workers=1"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.12.12",
    "otplib": "^12.0.1",
    "typescript": "^5.4.5",
    "wait-on": "^7.2.0"
  }
}
