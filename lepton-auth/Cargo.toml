[package]
name = "lepton-auth"
version = "0.1.0"
edition = "2021"
license.workspace = true
repository.workspace = true
description = "Auth server functions, password policy, factors, OAuth, and token helpers"
publish = false

[dependencies]
leptos = { workspace = true }
leptos_router = { workspace = true }
leptos_axum = { workspace = true, optional = true }
axum = { workspace = true, optional = true }

http = { workspace = true }
cfg-if = { workspace = true }
thiserror = { workspace = true }
url = { workspace = true }
urlencoding = { workspace = true }

serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true, optional = true }
axum-login = { workspace = true, optional = true }
tower-sessions = { workspace = true, optional = true }
argon2 = { workspace = true, optional = true }
password-hash = { workspace = true, optional = true }
sha2 = { workspace = true, optional = true }
rand_core = { workspace = true, optional = true }
anyhow = { workspace = true, optional = true }

valence = { workspace = true, optional = true }
chrono = { workspace = true }
async-trait = { workspace = true, optional = true }
higgs = { workspace = true, optional = true }
higgs-macros = { workspace = true, optional = true }
lepton-smtp = { workspace = true, optional = true }
lepton-sms = { workspace = true, optional = true }
lepton-spectra-telemetry = { workspace = true, optional = true }
lepton-host-adapter = { workspace = true, optional = true }
lepton-identity = { workspace = true, optional = true }
photon = { workspace = true, optional = true }
photon-leptos = { workspace = true, optional = true, default-features = false }
totp-rs = { workspace = true, optional = true }
qrcode = { version = "0.14", default-features = false, features = ["svg"], optional = true }
tracing = { workspace = true, optional = true }
futures = { workspace = true, optional = true }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"], optional = true }
webauthn-rs = { workspace = true, optional = true }
uuid = { workspace = true, optional = true }
boson-core = { workspace = true, optional = true }
boson-macros = { workspace = true, optional = true }
boson-runtime = { workspace = true, optional = true }
boson-valence-identity = { workspace = true, optional = true }
quark = { workspace = true, optional = true }

wasm-bindgen = { workspace = true, optional = true }
wasm-bindgen-futures = { workspace = true, optional = true }
js-sys = { workspace = true, optional = true }
web-sys = { workspace = true, optional = true, features = [
    "Window",
    "Navigator",
    "CredentialsContainer",
] }

[lints]
workspace = true

[[test]]
name = "auth_boundaries"
required-features = ["ssr"]

[[test]]
name = "verification_tokens"
required-features = ["ssr", "test-utils", "phone"]

[[example]]
name = "password_and_token"
required-features = ["ssr"]

[[example]]
name = "auth_flows_noop_smtp"
required-features = ["ssr", "email"]

[[example]]
name = "auth_flows_smtp_mailpit"
required-features = ["ssr", "email"]

[[example]]
name = "auth_contacts_confirm"
required-features = ["ssr"]

[[example]]
name = "auth_trust_confirm"
required-features = ["ssr"]

[[example]]
name = "auth_totp_enroll"
required-features = ["ssr", "totp"]

[[example]]
name = "step_up_totp"
required-features = ["ssr", "totp"]

[[example]]
name = "auth_devices"
required-features = ["ssr"]

[[example]]
name = "auth_webauthn"
required-features = ["ssr", "webauthn"]

[[example]]
name = "auth_oauth_mock"
required-features = ["ssr", "oauth-github"]

[[test]]
name = "oauth_link_settings"
required-features = ["ssr"]

[[test]]
name = "oauth_pkce_state"
required-features = ["ssr", "oauth-google"]

[[test]]
name = "webauthn_devices"
required-features = ["ssr", "webauthn"]

[[test]]
name = "account_primary_email"
required-features = ["ssr"]

[[test]]
name = "contact_validators"
required-features = ["ssr"]

[[test]]
name = "password_hash_privacy"
required-features = ["ssr"]

[[test]]
name = "change_password"
required-features = ["ssr"]

[[test]]
name = "account_wipe"
required-features = ["ssr", "totp"]

[[test]]
name = "totp_recovery"
required-features = ["ssr", "totp"]

[[test]]
name = "confirm_account_status"
required-features = ["ssr", "phone"]

[[test]]
name = "membership_primary_clear"
required-features = ["ssr", "test-utils", "phone"]

[features]
default = []
hydrate = [
    "leptos/hydrate",
    "dep:web-sys",
    "dep:wasm-bindgen",
    "dep:wasm-bindgen-futures",
    "dep:js-sys",
    "dep:photon-leptos",
    "dep:higgs-macros",
    "photon-leptos/hydrate",
]
# Enables publish-capture helpers for validating Photon hooks in integration tests.
test-utils = ["lepton-identity?/test-utils"]
ssr = [
    "leptos/ssr",
    "leptos_router/ssr",
    "dep:leptos_axum",
    "dep:tokio",
    "dep:axum",
    "dep:axum-login",
    "dep:tower-sessions",
    "dep:argon2",
    "dep:password-hash",
    "dep:sha2",
    "dep:rand_core",
    "dep:anyhow",
    "dep:valence",
    "dep:async-trait",
    "dep:higgs",
    "dep:higgs-macros",
    "dep:lepton-host-adapter",
    "dep:lepton-identity",
    "dep:photon",
    "dep:photon-leptos",
    "dep:tracing",
    "dep:futures",
    "dep:reqwest",
    "lepton-host-adapter/ssr",
    "higgs/ssr",
    "photon/runtime",
    "photon-leptos/ssr",
]
# Verification / reset mail delivery (not email-as-login).
email = ["dep:lepton-smtp"]
# SMS OTP + lepton-sms adapters.
phone = ["dep:lepton-sms"]
# TOTP verify / enroll QR (totp-rs + qrcode SVG on SSR).
totp = ["dep:totp-rs", "dep:qrcode"]
# Alias for totp.
two_factor = ["totp"]
# WebAuthn passkey ceremony for AuthDevice kind=webauthn (server-side verify).
webauthn = ["dep:webauthn-rs", "dep:uuid"]
# OAuth provider flags (cfg only). Live HTTP exchange needs `ssr` (which already
# enables `reqwest`). Mock provider works with `ssr` alone via
# `OAuthClientConfig::use_mock_provider`.
oauth-google = []
oauth-github = []
# Durable Boson delivery + DeliveryAttempt log (requires host Boson + DeliveryRuntime).
boson-delivery = [
    "dep:boson-core",
    "dep:boson-macros",
    "dep:boson-runtime",
    "dep:boson-valence-identity",
    "dep:quark",
    "dep:uuid",
]
# All Ship channels + OAuth flags + WebAuthn.
full = ["email", "phone", "totp", "oauth-google", "oauth-github", "webauthn"]
# Emit auth funnel Spectra counters / failure events via lepton-spectra-telemetry.
spectra = ["dep:lepton-spectra-telemetry"]

[[test]]
name = "delivery_attempt"
required-features = ["ssr", "boson-delivery", "email"]

[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
wiremock = "0.6"
serde_json = { workspace = true }
webauthn-authenticator-rs = { version = "0.5.5", default-features = false, features = ["softpasskey"] }
url = { workspace = true }
boson-backend-mem = { workspace = true }
boson-core = { workspace = true }
boson-runtime = { workspace = true }
boson-valence-identity = { workspace = true }
